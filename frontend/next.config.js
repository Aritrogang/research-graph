/** @type {import('next').NextConfig} */
const nextConfig = {
  // No "standalone" — Vercel manages its own build output.
  // NEXT_PUBLIC_API_URL is set in the Vercel dashboard.
};

module.exports = nextConfig;
